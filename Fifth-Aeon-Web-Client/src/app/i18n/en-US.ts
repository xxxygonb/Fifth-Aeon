/**
 * en-US UI dictionary. English is the source language: strings resolve to
 * themselves, so this map only needs entries that must override the source.
 */
export const enUSUI: Record<string, string> = {};
